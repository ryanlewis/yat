# Architecture notes

No frontmatter at all. Just a plain markdown file that lives
alongside the items. yat should skip this silently.

## Dependencies
- Postgres 15+
- Redis 7+

status: this line looks like YAML but it isn't in frontmatter.
